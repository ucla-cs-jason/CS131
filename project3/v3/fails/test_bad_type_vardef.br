func foo(x:string): string {
  var y;
  if (true) {
    var x: int;
    x = 5;
    print(x);
  }
  print(x);
}

func main(): void {
  var a: string;
  a = "foo";
  foo(a);
  print(a);
}

/*
*OUT*
ErrorType.TYPE_ERROR
*OUT*
*/
